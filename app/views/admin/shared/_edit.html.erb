<% className = form_action.delete(:class) %>
<%= form_tag(form_action, :id => "#{form_type}_form", :enctype => "multipart/form-data", :class => className) do %>

  <%= render :partial => "form" %>

<% end %>

<%= render :partial => "merge_articles" if @current_user.profile_id == 1 && controller_name == 'content' && action_name == 'edit' %>

